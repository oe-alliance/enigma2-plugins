<setupxml>
	<setup key="autotimer" title="Autotimer Settings" showOpenWebIf="1">
		<item level="0" text="Poll automatically" description="Unless this is enabled AutoTimer will NOT automatically look for events matching your AutoTimers but only when you leave the GUI with the green button.">config.plugins.autotimer.autopoll</item>
		<item level="0" text="Only poll while in standby" description="When this is enabled AutoTimer will ONLY check for new events whilst in standby.">config.plugins.autotimer.onlyinstandby</item>
		<item level="0" text="Startup delay (in min)" description="This is the delay in minutes that the AutoTimer will wait on initial launch to not delay enigma2 startup time.">config.plugins.autotimer.delay</item>
		<item level="0" text="Poll Interval (in mins)" description="This is the delay in minutes that the AutoTimer will wait after a search to search the EPG again.">config.plugins.autotimer.interval</item>
		<item level="0" text="Only add timer for next x days" description="You can control for how many days in the future timers are added. Set this to 0 to disable this feature.">config.plugins.autotimer.maxdaysinfuture</item>
		<item level="0" text="Allow double timer for different services" description="Here you can specify whether simultaneous timers of the same program can be created. This allows simultaneous recording of a program with different resolutions. (e.g. SD service and HD service)">config.plugins.autotimer.enable_multiple_timer</item>
		<item level="0" text="Include streams in search" description="Select 'yes' to include IPTV streams in the search.">config.plugins.autotimer.include_streams</item>
		<item level="0" text="Show in plugin browser" description="Enable this to be able to access the AutoTimer Overview from within the plugin browser.">config.plugins.autotimer.show_in_plugins</item>
		<item level="0" text="Show in extension menu" description="Enable this to be able to access the AutoTimer Overview from within the extension menu.">config.plugins.autotimer.show_in_extensionsmenu</item>
		<item level="0" text="Modify existing timers" description="This setting controls the behavior when a timer matches a found event.">config.plugins.autotimer.refresh</item>
		<item level="0" text="Guess existing timer based on begin/end" description="If this is enabled an existing timer will also be considered recording an event if it records at least 80% of the it.">config.plugins.autotimer.try_guessing</item>
		<item level="0" text="Add similar timer on conflict" description="If a timer conflict occurs, AutoTimer will search outside the timespan for a similar event and add it.">config.plugins.autotimer.addsimilar_on_conflict</item>
		<item level="0" text="Add timer as disabled on conflict" description="This toggles the behavior on timer conflicts. If an AutoTimer matches an event that conflicts with an existing timer it will not ignore this event but add it disabled.">config.plugins.autotimer.disabled_on_conflict</item>
		<item level="0" text="Include 'AutoTimer' in tags" description="If this is selected, the tag 'AutoTimer' will be given to timers created by this plugin.">config.plugins.autotimer.add_autotimer_to_tags</item>
		<item level="0" text="Include AutoTimer name in tags" description="If this is selected, the name of the respective AutoTimer will be added as a tag to timers created by this plugin.">config.plugins.autotimer.add_name_to_tags</item>
		<item level="0" text="Show notification on conflicts" description="By enabling this you will be notified about timer conflicts found during automated polling. There is no intelligence involved, so it might bother you about the same conflict over and over.">config.plugins.autotimer.notifconflict</item>
		<item level="0" text="Show notification on similars" description="By enabling this you will be notified about similar timers added during automated polling. There is no intelligence involved, so it might bother you about the same conflict over and over.">config.plugins.autotimer.notifsimilar</item>
		<item level="0" text="Editor for new AutoTimers" description="The editor to be used for new AutoTimers. This can either be the Wizard or the classic editor.">config.plugins.autotimer.editor</item>
		<item level="0" text="Support 'Fast Scan'?" description="When supporting 'Fast Scan' the service type is ignored. You dont need to enable this unless your Image supports 'Fast Scan' and you are using it.">config.plugins.autotimer.fastscan</item>
		<item level="0" text="Skip poll during records" description="If enabled, the polling will be skipped if a recording is in progress.">config.plugins.autotimer.skip_during_records</item>
		<item level="0" text="Skip poll during epg refresh" description="If enabled, the polling will be skipped if EPGRefresh is currently running.">config.plugins.autotimer.skip_during_epgrefresh</item>
		<item level="0" text="Popup timeout in seconds" description="If 0, the popup will remain open.">config.plugins.autotimer.popup_timeout</item>
		<item level="0" text="Remove not existing events" description="Check the event id (eit) and remove the timer if there is no corresponding EPG event. Due to compatibility issues with SerienRecorder and IPRec, only timer created by AutoTimer are affected.">config.plugins.autotimer.check_eit_and_remove</item>
		<item level="0" text="Always write config" description="Write the config file after every change which the user quits by saving.">config.plugins.autotimer.always_write_config</item>
	</setup>
</setupxml>
